import { EmploymentSection } from './employment-section.model';

describe('EmploymentSection', () => {
  it('should create an instance', () => {
    expect(new EmploymentSection()).toBeTruthy();
  });
});
